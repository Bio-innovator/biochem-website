/**
 * ============================================
 * BioChem Niche - 全局样式
 * 深蓝/青色科技学术主题
 * ============================================
 */

/* ==================== CSS变量 ==================== */
:root {
--bg-primary: #0a1628;
--bg-secondary: #111d35;
--bg-card: #1a2744;
--bg-card-hover: #223555;
--cyan: #4ecdc4;
--blue: #45b7d1;
--cyan-light: #5ee7df;
--text-primary: #e0e6ed;
--text-secondary: #8b9dc3;
--text-muted: #5a6b8a;
--success: #2ecc71;
--danger: #e74c3c;
--warning: #f39c12;
--border: #2a3a5a;
--grad-cyan: linear-gradient(135deg, #4ecdc4, #45b7d1);
--shadow: 0 4px 20px rgba(0,0,0,0.3);
--shadow-hover: 0 8px 30px rgba(78,205,196,0.15);
--transition: all 0.3s ease;
--font-serif: 'Times New Roman', Georgia, 'SimSun', 'Songti SC', serif;
--font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

/* ==================== 全局重置 ==================== */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth;font-size:16px}
body{font-family:var(--font-serif);background:var(--bg-primary);color:var(--text-primary);line-height:1.6;min-height:100vh;-webkit-font-smoothing:antialiased}
:lang(zh),[data-zh]{font-family:'SimSun','Songti SC','STSong',serif}
a{color:var(--cyan);text-decoration:none;transition:var(--transition)}
a:hover{color:var(--cyan-light)}

/* ==================== 顶部导航 ==================== */
.top-nav{position:fixed;top:0;left:0;right:0;height:60px;background:linear-gradient(180deg,var(--bg-primary),var(--bg-secondary));border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;padding:0 1.5rem;z-index:1000;backdrop-filter:blur(10px)}
.nav-brand{display:flex;align-items:center;gap:0.5rem;font-size:1.25rem;font-weight:700;color:var(--cyan);cursor:pointer;font-family:'Times New Roman',serif;letter-spacing:0.5px}
.nav-brand .brand-icon{font-size:1.4rem}
.nav-links{display:flex;gap:0.3rem}
.nav-link{padding:0.45rem 0.9rem;border-radius:6px;color:var(--text-secondary);font-size:0.9rem;transition:var(--transition);border:1px solid transparent;white-space:nowrap}
.nav-link:hover{color:var(--text-primary);background:rgba(78,205,196,0.08)}
.nav-link.active{color:var(--cyan);background:rgba(78,205,196,0.12);border-color:rgba(78,205,196,0.3)}
.nav-controls{display:flex;align-items:center;gap:0.6rem}
.lang-toggle{background:var(--bg-card);border:1px solid var(--border);color:var(--cyan);padding:0.35rem 0.7rem;border-radius:6px;cursor:pointer;font-size:0.8rem;transition:var(--transition)}
.lang-toggle:hover{background:rgba(78,205,196,0.15);border-color:var(--cyan)}
.mobile-menu-btn{display:none;flex-direction:column;gap:4px;background:none;border:none;cursor:pointer;padding:5px}
.mobile-menu-btn span{display:block;width:22px;height:2px;background:var(--text-primary);transition:var(--transition)}

/* ==================== 主内容 ==================== */
.main-container{padding-top:60px;min-height:100vh}
.module{padding:1.5rem 2rem;max-width:1400px;margin:0 auto}
.module.hidden{display:none}
.module-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem;padding-bottom:0.8rem;border-bottom:1px solid var(--border);flex-wrap:wrap;gap:1rem}
.module-header h2{font-family:'Times New Roman',serif;color:var(--cyan);font-size:1.35rem}
.progress-tracker{display:flex;align-items:center;gap:0.6rem}
.progress-bar{width:140px;height:7px;background:var(--bg-card);border-radius:4px;overflow:hidden}
.progress-fill{height:100%;background:var(--grad-cyan);border-radius:4px;transition:width 0.5s ease}
.progress-text{font-size:0.82rem;color:var(--text-secondary)}

/* ==================== 首页 ==================== */
.hero-section{text-align:center;padding:2.5rem 1rem 2rem}
.hero-title{font-family:'Times New Roman',serif;font-size:2.2rem;background:var(--grad-cyan);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;margin-bottom:0.8rem;line-height:1.3}
.hero-subtitle{color:var(--text-secondary);font-size:1rem;max-width:680px;margin:0 auto 1.5rem;line-height:1.7}
.hero-buttons{display:flex;gap:0.8rem;justify-content:center;margin-bottom:2rem;flex-wrap:wrap}
.hero-stats{display:flex;justify-content:center;gap:1.5rem;flex-wrap:wrap}
.stat-card{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1.25rem 2rem;text-align:center;transition:var(--transition)}
.stat-card:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover);transform:translateY(-3px)}
.stat-number{font-size:1.8rem;font-weight:700;color:var(--cyan);font-family:'Times New Roman',serif}
.stat-label{color:var(--text-secondary);font-size:0.85rem;margin-top:0.2rem}

.features-section{padding:1.5rem 0}
.section-title{text-align:center;color:var(--text-primary);font-size:1.25rem;margin-bottom:1.25rem;font-family:'Times New Roman',serif}
.feature-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:1.2rem}
.feature-card{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1.75rem;text-align:center;cursor:pointer;transition:var(--transition)}
.feature-card:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover);transform:translateY(-5px)}
.feature-icon{font-size:2.2rem;margin-bottom:0.75rem}
.feature-card h3{color:var(--cyan);margin-bottom:0.5rem;font-size:1.05rem}
.feature-card p{color:var(--text-secondary);font-size:0.85rem;line-height:1.55}

/* ==================== 按钮 ==================== */
.btn{display:inline-flex;align-items:center;justify-content:center;gap:0.4rem;padding:0.55rem 1.4rem;border-radius:8px;border:1px solid transparent;cursor:pointer;font-size:0.9rem;font-family:inherit;transition:var(--transition);outline:none}
.btn-primary{background:var(--grad-cyan);color:var(--bg-primary);font-weight:600}
.btn-primary:hover{opacity:0.9;transform:translateY(-2px);box-shadow:0 4px 15px rgba(78,205,196,0.3)}
.btn-secondary{background:transparent;border-color:var(--cyan);color:var(--cyan)}
.btn-secondary:hover{background:rgba(78,205,196,0.1)}
.btn-danger{background:rgba(231,76,60,0.15);border-color:var(--danger);color:var(--danger)}
.btn-danger:hover{background:rgba(231,76,60,0.25)}
.btn-small{padding:0.25rem 0.55rem;font-size:0.78rem}
.btn-large{padding:0.75rem 2.5rem;font-size:1rem}
.btn:disabled{opacity:0.5;cursor:not-allowed}

/* ==================== 搜索框 ==================== */
.search-box input{width:100%;padding:0.6rem 1rem;border:1px solid var(--border);border-radius:8px;background:var(--bg-card);color:var(--text-primary);font-family:inherit;font-size:0.9rem;transition:var(--transition);outline:none}
.search-box input:focus{border-color:var(--cyan);box-shadow:0 0 0 3px rgba(78,205,196,0.1)}
.search-box input::placeholder{color:var(--text-muted)}

/* ==================== 知识点模块 ==================== */
.knowledge-layout{display:grid;grid-template-columns:260px 1fr;gap:1.25rem}
.category-nav{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1rem;position:sticky;top:75px;height:fit-content;max-height:calc(100vh - 90px);overflow-y:auto}
.category-nav::-webkit-scrollbar{width:5px}
.category-nav::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px}
.category-tree{margin-top:0.8rem}
.category-group{margin-bottom:0.4rem}
.category-header{display:flex;align-items:center;justify-content:space-between;padding:0.5rem 0.6rem;border-radius:6px;cursor:pointer;color:var(--text-primary);font-weight:600;transition:var(--transition);font-size:0.85rem}
.category-header:hover{background:rgba(78,205,196,0.08)}
.category-header .toggle-icon{color:var(--text-muted);transition:transform 0.3s;font-size:0.75rem}
.category-header.expanded .toggle-icon{transform:rotate(90deg)}
.category-topics{padding-left:0.5rem;overflow:hidden;max-height:0;transition:max-height 0.3s ease}
.category-topics.expanded{max-height:2000px}
.topic-item{padding:0.35rem 0.6rem;color:var(--text-secondary);cursor:pointer;border-radius:4px;font-size:0.82rem;transition:var(--transition);border-left:2px solid transparent}
.topic-item:hover{color:var(--cyan);background:rgba(78,205,196,0.05);border-left-color:var(--cyan)}
.knowledge-filters{display:flex;gap:0.4rem;margin-bottom:0.8rem;flex-wrap:wrap}
.filter-btn{padding:0.35rem 0.85rem;border-radius:6px;border:1px solid var(--border);background:var(--bg-card);color:var(--text-secondary);cursor:pointer;font-family:inherit;font-size:0.82rem;transition:var(--transition)}
.filter-btn:hover{border-color:var(--cyan);color:var(--cyan)}
.filter-btn.active{background:rgba(78,205,196,0.15);border-color:var(--cyan);color:var(--cyan)}
.knowledge-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:0.85rem}
.knowledge-card{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1.15rem;cursor:pointer;transition:var(--transition);position:relative;overflow:hidden}
.knowledge-card::before{content:'';position:absolute;top:0;left:0;width:3px;height:100%;background:var(--grad-cyan);opacity:0;transition:opacity 0.3s}
.knowledge-card:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover);transform:translateY(-3px)}
.knowledge-card:hover::before{opacity:1}
.knowledge-card.favorited{border-color:rgba(243,156,18,0.4)}
.knowledge-card .card-title{font-size:0.95rem;color:var(--cyan);margin-bottom:0.2rem;font-family:'Times New Roman',serif;font-weight:600}
.knowledge-card .card-title-cn{font-size:0.88rem;color:var(--text-primary);margin-bottom:0.3rem}
.knowledge-card .card-summary{color:var(--text-secondary);font-size:0.82rem;line-height:1.5;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}
.knowledge-card .card-meta{display:flex;justify-content:space-between;align-items:center;margin-top:0.6rem;padding-top:0.6rem;border-top:1px solid var(--border)}
.knowledge-card .card-tags{display:flex;gap:0.3rem;flex-wrap:wrap}
.knowledge-card .card-tag{font-size:0.72rem;padding:0.12rem 0.45rem;background:rgba(78,205,196,0.1);color:var(--cyan);border-radius:4px}
.fav-btn{background:none;border:none;cursor:pointer;font-size:1rem;padding:0.2rem;opacity:0.5;transition:var(--transition);line-height:1}
.fav-btn:hover{opacity:1;transform:scale(1.2)}
.fav-btn.active{opacity:1}

/* 知识点弹窗 */
.knowledge-modal{max-width:700px;max-height:85vh;overflow-y:auto}
.knowledge-detail{padding:0.5rem 0}
.knowledge-detail .detail-header{margin-bottom:1.25rem;padding-bottom:0.8rem;border-bottom:1px solid var(--border)}
.knowledge-detail .detail-title{font-size:1.25rem;color:var(--cyan);font-family:'Times New Roman',serif;margin-bottom:0.3rem}
.knowledge-detail .detail-title-cn{font-size:1.05rem;color:var(--text-primary)}
.knowledge-detail .detail-section{margin-bottom:1.1rem}
.knowledge-detail .detail-section h4{color:var(--blue);margin-bottom:0.4rem;font-size:0.95rem}
.knowledge-detail .detail-section p{color:var(--text-secondary);line-height:1.65;font-size:0.88rem}
.knowledge-detail .keypoints-list{list-style:none;padding:0}
.knowledge-detail .keypoints-list li{padding:0.25rem 0;color:var(--text-secondary);position:relative;padding-left:1.1rem;font-size:0.88rem}
.knowledge-detail .keypoints-list li::before{content:'\25B8';position:absolute;left:0;color:var(--cyan)}
.knowledge-detail .formula-box{background:rgba(78,205,196,0.08);border:1px solid rgba(78,205,196,0.3);border-radius:8px;padding:0.85rem;font-family:'Times New Roman',serif;color:var(--cyan);text-align:center;font-size:0.95rem}
.knowledge-detail .detail-actions{display:flex;gap:0.75rem;margin-top:1.25rem;padding-top:0.8rem;border-top:1px solid var(--border);flex-wrap:wrap}

/* ==================== 小测模块 ==================== */
.quiz-start{max-width:580px;margin:0 auto;text-align:center}
.quiz-options{margin:1.5rem 0;text-align:left}
.quiz-option-group{margin-bottom:1.25rem}
.quiz-option-group h3{color:var(--text-secondary);font-size:0.9rem;margin-bottom:0.6rem}
.option-buttons{display:flex;gap:0.4rem;flex-wrap:wrap}
.option-btn{padding:0.45rem 0.9rem;border-radius:6px;border:1px solid var(--border);background:var(--bg-card);color:var(--text-secondary);cursor:pointer;font-family:inherit;font-size:0.85rem;transition:var(--transition)}
.option-btn:hover{border-color:var(--cyan);color:var(--cyan)}
.option-btn.active{background:rgba(78,205,196,0.15);border-color:var(--cyan);color:var(--cyan)}
.quiz-shortcuts{display:flex;justify-content:center;gap:0.8rem;margin-top:1.5rem}
.shortcut-btn{display:flex;align-items:center;gap:0.4rem;padding:0.55rem 1rem;background:var(--bg-card);border:1px solid var(--border);border-radius:8px;color:var(--text-primary);cursor:pointer;font-family:inherit;font-size:0.85rem;transition:var(--transition);position:relative}
.shortcut-btn:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover)}
.shortcut-btn .badge{position:absolute;top:-7px;right:-7px;background:var(--danger);color:#fff;font-size:0.7rem;padding:0.1rem 0.4rem;border-radius:10px;font-family:'Times New Roman',serif}
.quiz-interface{max-width:780px;margin:0 auto}
.quiz-status-bar{display:flex;justify-content:space-between;align-items:center;margin-bottom:0.8rem;padding:0.6rem 1rem;background:var(--bg-card);border-radius:8px;border:1px solid var(--border)}
.quiz-progress-bar{height:5px;background:var(--bg-card);border-radius:3px;margin-bottom:1.25rem;overflow:hidden}
.quiz-progress-fill{height:100%;background:var(--grad-cyan);border-radius:3px;transition:width 0.4s ease}
.quiz-question-area{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1.5rem;margin-bottom:1.25rem}
.question-text{font-size:1.05rem;color:var(--text-primary);margin-bottom:0.8rem;line-height:1.65;font-family:'Times New Roman',serif}
.question-text-cn{font-size:0.95rem;color:var(--text-secondary);margin-bottom:1.25rem;line-height:1.55}
.answer-options{display:flex;flex-direction:column;gap:0.6rem}
.answer-option{display:flex;align-items:center;gap:0.8rem;padding:0.85rem 1rem;border:1px solid var(--border);border-radius:8px;cursor:pointer;transition:var(--transition);background:var(--bg-secondary)}
.answer-option:hover{border-color:var(--cyan);background:rgba(78,205,196,0.05)}
.answer-option .option-letter{width:30px;height:30px;display:flex;align-items:center;justify-content:center;border-radius:50%;background:var(--border);color:var(--text-primary);font-weight:700;font-family:'Times New Roman',serif;font-size:0.85rem;flex-shrink:0}
.answer-option .option-text{color:var(--text-primary);font-family:'Times New Roman',serif;font-size:0.9rem}
.answer-option.correct{border-color:var(--success);background:rgba(46,204,113,0.1)}
.answer-option.correct .option-letter{background:var(--success);color:#fff}
.answer-option.wrong{border-color:var(--danger);background:rgba(231,76,60,0.1)}
.answer-option.wrong .option-letter{background:var(--danger);color:#fff}
.answer-option.disabled{pointer-events:none;opacity:0.7}
.explanation-box{margin-top:1.25rem;padding:1rem;background:rgba(78,205,196,0.06);border:1px solid rgba(78,205,196,0.25);border-radius:8px}
.explanation-box .explanation-label{color:var(--cyan);font-weight:600;margin-bottom:0.4rem;display:block}
.explanation-box p{color:var(--text-secondary);line-height:1.65;font-size:0.85rem}
.quiz-controls{display:flex;justify-content:center;gap:0.75rem}
.quiz-result{max-width:560px;margin:0 auto;text-align:center}
.result-card{background:var(--bg-card);border:1px solid var(--border);border-radius:16px;padding:2rem}
.result-card h2{color:var(--cyan);margin-bottom:1.25rem}
.result-stats{display:flex;justify-content:center;gap:1.5rem;margin-bottom:1.25rem;flex-wrap:wrap}
.result-stat{text-align:center}
.result-value{font-size:1.8rem;font-weight:700;color:var(--cyan);font-family:'Times New Roman',serif}
.result-label{color:var(--text-secondary);font-size:0.85rem;margin-top:0.15rem}
.result-feedback{margin:1.25rem 0;padding:0.85rem;border-radius:8px;font-size:0.95rem}
.result-feedback.excellent{background:rgba(46,204,113,0.1);color:var(--success)}
.result-feedback.good{background:rgba(243,156,18,0.1);color:var(--warning)}
.result-feedback.needs-work{background:rgba(231,76,60,0.1);color:var(--danger)}
.result-actions{display:flex;justify-content:center;gap:0.6rem;flex-wrap:wrap}
.quiz-modal{max-width:680px;max-height:80vh;overflow-y:auto}
.quiz-modal h3{color:var(--cyan);margin-bottom:0.8rem}
.wrong-item,.stat-item{background:var(--bg-secondary);border:1px solid var(--border);border-radius:8px;padding:0.85rem;margin-bottom:0.6rem}
.wrong-item .wrong-q{color:var(--text-primary);font-family:'Times New Roman',serif;margin-bottom:0.4rem;font-size:0.9rem}
.wrong-item .correct-a{color:var(--success);font-size:0.82rem}
.stat-item{display:flex;justify-content:space-between;align-items:center}
.stat-item .stat-label{color:var(--text-secondary);font-size:0.88rem}
.stat-item .stat-value{color:var(--cyan);font-weight:600;font-family:'Times New Roman',serif}

/* ==================== 词汇模块 ==================== */
.glossary-controls{display:flex;justify-content:space-between;align-items:center;margin-bottom:0.8rem;gap:1rem;flex-wrap:wrap}
.glossary-search{flex:1;min-width:220px;max-width:380px}
.glossary-filters{display:flex;gap:0.4rem}
.az-index{display:flex;flex-wrap:wrap;gap:0.3rem;margin-bottom:1.25rem;padding:0.6rem;background:var(--bg-card);border-radius:8px;border:1px solid var(--border)}
.az-letter{width:26px;height:26px;display:flex;align-items:center;justify-content:center;border-radius:4px;background:var(--bg-secondary);color:var(--text-secondary);cursor:pointer;font-family:'Times New Roman',serif;font-size:0.78rem;font-weight:600;border:1px solid var(--border);transition:var(--transition)}
.az-letter:hover{background:rgba(78,205,196,0.15);color:var(--cyan);border-color:var(--cyan)}
.az-letter.active{background:var(--cyan);color:var(--bg-primary);border-color:var(--cyan)}
.az-letter.disabled{opacity:0.25;cursor:default;pointer-events:none}
.daily-five{margin-bottom:1.75rem}
.daily-five h3{display:flex;align-items:center;gap:0.6rem;color:var(--text-primary);margin-bottom:0.8rem;font-size:1.05rem}
.daily-cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:0.7rem}
.daily-card{background:var(--bg-card);border:1px solid var(--border);border-radius:10px;padding:1.1rem;position:relative;transition:var(--transition)}
.daily-card:hover{border-color:var(--cyan);transform:translateY(-3px)}
.daily-card .daily-word{font-family:'Times New Roman',serif;color:var(--cyan);font-size:1rem;font-weight:600;margin-bottom:0.15rem}
.daily-card .daily-phonetic{color:var(--text-muted);font-size:0.78rem;font-family:'Times New Roman',serif;margin-bottom:0.25rem}
.daily-card .daily-meaning{color:var(--text-secondary);font-size:0.85rem}
.daily-card .vocab-action{position:absolute;top:0.6rem;right:0.6rem;background:none;border:none;cursor:pointer;font-size:0.95rem;opacity:0.5;transition:opacity 0.2s}
.daily-card .vocab-action:hover{opacity:1}
.glossary-list{display:grid;grid-template-columns:repeat(auto-fill,minmax(330px,1fr));gap:0.7rem}
.glossary-item{background:var(--bg-card);border:1px solid var(--border);border-radius:10px;padding:1.1rem;transition:var(--transition);position:relative}
.glossary-item:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover)}
.glossary-item.in-notebook{border-color:rgba(243,156,18,0.3)}
.glossary-item .vocab-word{font-family:'Times New Roman',serif;color:var(--cyan);font-size:1.05rem;font-weight:600;margin-bottom:0.15rem}
.glossary-item .vocab-phonetic{color:var(--text-muted);font-size:0.82rem;font-family:'Times New Roman',serif;margin-bottom:0.3rem}
.glossary-item .vocab-meaning{color:var(--text-primary);font-size:0.92rem;margin-bottom:0.4rem}
.glossary-item .vocab-category{display:inline-block;font-size:0.72rem;padding:0.18rem 0.45rem;border-radius:4px;margin-bottom:0.4rem;margin-right:0.3rem}
.glossary-item .vocab-category.bio{background:rgba(46,204,113,0.15);color:var(--success)}
.glossary-item .vocab-category.chem{background:rgba(69,183,209,0.15);color:var(--blue)}
.glossary-item .vocab-example{color:var(--text-secondary);font-size:0.82rem;line-height:1.5;font-style:italic;font-family:'Times New Roman',serif;border-left:2px solid var(--border);padding-left:0.7rem}
.glossary-item .vocab-actions{position:absolute;top:0.65rem;right:0.65rem}

/* ==================== 专业探索模块 ==================== */
.majors-tabs{display:flex;gap:0.5rem;margin-bottom:1.25rem;border-bottom:1px solid var(--border);padding-bottom:0.5rem}
.major-tab{padding:0.55rem 1.25rem;border-radius:8px 8px 0 0;border:1px solid transparent;border-bottom:none;background:transparent;color:var(--text-secondary);cursor:pointer;font-family:inherit;font-size:0.9rem;transition:var(--transition);position:relative;top:1px}
.major-tab:hover{color:var(--cyan)}
.major-tab.active{background:var(--bg-card);border-color:var(--border);color:var(--cyan);border-bottom:1px solid var(--bg-card)}
.majors-search-box{max-width:400px;margin-bottom:1.25rem}
.majors-search-box input{width:100%;padding:0.6rem 1rem;border:1px solid var(--border);border-radius:8px;background:var(--bg-card);color:var(--text-primary);font-family:inherit;font-size:0.9rem;outline:none;transition:var(--transition)}
.majors-search-box input:focus{border-color:var(--cyan);box-shadow:0 0 0 3px rgba(78,205,196,0.1)}
.majors-panel{display:block}
.majors-panel.hidden{display:none}
.tips-section{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:0.8rem;margin-bottom:1.5rem}
.tip-card{background:linear-gradient(135deg,rgba(69,183,209,0.08),rgba(78,205,196,0.05));border:1px solid rgba(69,183,209,0.25);border-radius:10px;padding:1rem;cursor:pointer;transition:var(--transition)}
.tip-card:hover{border-color:var(--blue);box-shadow:var(--shadow-hover);transform:translateY(-2px)}
.tip-card .tip-title{color:var(--blue);font-size:0.92rem;font-weight:600;margin-bottom:0.35rem;font-family:'Times New Roman',serif}
.tip-card .tip-desc{color:var(--text-secondary);font-size:0.82rem;line-height:1.5}
.majors-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:0.85rem}
.major-card{background:var(--bg-card);border:1px solid var(--border);border-radius:12px;padding:1.25rem;cursor:pointer;transition:var(--transition);position:relative;overflow:hidden}
.major-card::before{content:'';position:absolute;top:0;left:0;width:100%;height:3px;background:var(--grad-cyan);opacity:0;transition:opacity 0.3s}
.major-card:hover{border-color:var(--cyan);box-shadow:var(--shadow-hover);transform:translateY(-4px)}
.major-card:hover::before{opacity:1}
.major-card.undergrad::before{background:linear-gradient(90deg,var(--blue),#3a8bc8)}
.major-card.grad::before{background:linear-gradient(90deg,var(--cyan),#3dd4c0)}
.major-card.interested{border-color:rgba(243,156,18,0.4)}
.major-card.interested::after{content:'\2B50';position:absolute;top:0.6rem;right:0.6rem;font-size:0.85rem}
.major-card .major-name{font-family:'Times New Roman',serif;color:var(--cyan);font-size:1.05rem;font-weight:600;margin-bottom:0.2rem}
.major-card .major-name-cn{color:var(--text-primary);font-size:0.9rem;margin-bottom:0.4rem}
.major-card .major-desc{color:var(--text-secondary);font-size:0.83rem;line-height:1.55;margin-bottom:0.6rem;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}
.major-card .major-tags{display:flex;gap:0.3rem;flex-wrap:wrap}
.major-card .major-tag{font-size:0.72rem;padding:0.12rem 0.45rem;border-radius:4px;background:rgba(78,205,196,0.1);color:var(--cyan)}
.major-card .interest-btn{position:absolute;top:0.6rem;right:0.6rem;background:none;border:none;cursor:pointer;font-size:1rem;opacity:0.5;transition:var(--transition)}
.major-card .interest-btn:hover{opacity:1;transform:scale(1.15)}

/* 专业详情弹窗 */
.major-modal{max-width:720px;max-height:85vh;overflow-y:auto}
.major-detail{padding:0.5rem 0}
.major-detail .detail-header{margin-bottom:1.25rem;padding-bottom:0.8rem;border-bottom:1px solid var(--border)}
.major-detail .detail-name{font-size:1.25rem;color:var(--cyan);font-family:'Times New Roman',serif;font-weight:600;margin-bottom:0.2rem}
.major-detail .detail-name-cn{font-size:1.05rem;color:var(--text-primary);margin-bottom:0.3rem}
.major-detail .detail-type{display:inline-block;font-size:0.75rem;padding:0.2rem 0.55rem;border-radius:4px;margin-bottom:0.5rem}
.major-detail .detail-type.undergrad{background:rgba(69,183,209,0.15);color:var(--blue)}
.major-detail .detail-type.grad{background:rgba(78,205,196,0.15);color:var(--cyan)}
.major-detail .detail-section{margin-bottom:1.1rem}
.major-detail .detail-section h4{color:var(--blue);font-size:0.95rem;margin-bottom:0.4rem;display:flex;align-items:center;gap:0.4rem}
.major-detail .detail-section p{color:var(--text-secondary);font-size:0.88rem;line-height:1.65}
.major-detail .detail-section ul{list-style:none;padding:0}
.major-detail .detail-section ul li{padding:0.2rem 0;color:var(--text-secondary);font-size:0.87rem;position:relative;padding-left:1rem}
.major-detail .detail-section ul li::before{content:'\25B8';position:absolute;left:0;color:var(--cyan)}
.major-detail .detail-actions{display:flex;gap:0.75rem;margin-top:1.25rem;padding-top:0.8rem;border-top:1px solid var(--border);flex-wrap:wrap}

/* ==================== 弹窗通用 ==================== */
.modal-overlay{position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(10,22,40,0.88);backdrop-filter:blur(4px);display:flex;align-items:center;justify-content:center;z-index:2000;padding:1rem;animation:fadeIn 0.25s ease}
.modal-overlay.hidden{display:none}
.modal-content{background:var(--bg-secondary);border:1px solid var(--border);border-radius:14px;padding:1.75rem;width:100%;position:relative;animation:modalSlide 0.3s ease;box-shadow:0 20px 60px rgba(0,0,0,0.5)}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes modalSlide{from{opacity:0;transform:translateY(-15px) scale(0.97)}to{opacity:1;transform:translateY(0) scale(1)}}
.modal-close{position:absolute;top:0.8rem;right:0.8rem;background:none;border:none;color:var(--text-muted);font-size:1.4rem;cursor:pointer;width:30px;height:30px;display:flex;align-items:center;justify-content:center;border-radius:50%;transition:var(--transition);z-index:10}
.modal-close:hover{background:rgba(255,255,255,0.08);color:var(--text-primary)}

/* ==================== 页脚 ==================== */
.site-footer{text-align:center;padding:1.5rem;border-top:1px solid var(--border);color:var(--text-muted);font-size:0.82rem;margin-top:1.5rem}
.footer-content p{margin-bottom:0.3rem}
.footer-content a{color:var(--cyan)}
.footer-content a:hover{color:var(--cyan-light)}
.footer-copy{color:var(--text-muted);font-size:0.78rem;margin-top:0.4rem}

/* ==================== 回到顶部 ==================== */
.back-to-top{position:fixed;bottom:1.5rem;right:1.5rem;width:42px;height:42px;border-radius:50%;background:var(--bg-card);border:1px solid var(--border);color:var(--cyan);font-size:1.1rem;cursor:pointer;display:flex;align-items:center;justify-content:center;box-shadow:var(--shadow);transition:var(--transition);z-index:500}
.back-to-top:hover{background:var(--cyan);color:var(--bg-primary);transform:translateY(-3px)}
.back-to-top.hidden{display:none}

/* ==================== 响应式 ==================== */
@media(max-width:900px){
.knowledge-layout{grid-template-columns:1fr}
.category-nav{position:relative;top:0;max-height:none;order:2}
.nav-links{display:none;position:absolute;top:60px;left:0;right:0;background:var(--bg-secondary);flex-direction:column;padding:0.75rem;border-bottom:1px solid var(--border);gap:0}
.nav-links.open{display:flex}
.nav-link{padding:0.65rem 1rem}
.mobile-menu-btn{display:flex}
.hero-title{font-size:1.7rem}
.stat-card{padding:1rem 1.5rem}
.stat-number{font-size:1.5rem}
.module{padding:1rem}
}
@media(max-width:600px){
.top-nav{padding:0 1rem}
.hero-section{padding:1.5rem 0.5rem}
.hero-title{font-size:1.4rem}
.hero-subtitle{font-size:0.9rem}
.hero-buttons{flex-direction:column;align-items:center}
.knowledge-grid{grid-template-columns:1fr}
.glossary-list{grid-template-columns:1fr}
.glossary-controls{flex-direction:column;align-items:stretch}
.glossary-search{max-width:none}
.daily-cards{grid-template-columns:1fr}
.majors-grid{grid-template-columns:1fr}
.tips-section{grid-template-columns:1fr}
.modal-content{padding:1.25rem}
.quiz-modal,.knowledge-modal,.major-modal{max-height:90vh}
.result-actions{flex-direction:column}
.result-actions .btn{width:100%}
.quiz-shortcuts{flex-direction:column;align-items:center}
.majors-tabs{flex-wrap:wrap}
.feature-cards{grid-template-columns:1fr}
}

/* ==================== 滚动条美化 ==================== */
::-webkit-scrollbar{width:7px}
::-webkit-scrollbar-track{background:var(--bg-primary)}
::-webkit-scrollbar-thumb{background:var(--border);border-radius:4px}
::-webkit-scrollbar-thumb:hover{background:var(--cyan)}
::selection{background:rgba(78,205,196,0.3);color:var(--text-primary)}
